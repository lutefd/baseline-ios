# Baseline iOS Repository

This repository currently contains only the iOS app implementation:
- `apps/ios/Baseline`

Planned but external repositories:
- Go backend (sync + summaries)
- Wails desktop analytics

Shared integration contract lives here:
- `docs/contracts/api-v1.md`
